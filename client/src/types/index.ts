/**
 * Client Types Index
 *
 * @module ClientTypes
 * @description Central export file for all client-side TypeScript types and interfaces
 */

/**
 * Core types
 * @description Core types (Redux, hooks, base UI)
 */
export * from './core';

/**
 * Domain types
 * @description Domain-specific types (game, user, payment, admin, analytics)
 */
export * from './domain';

/**
 * Infrastructure types
 * @description Infrastructure types (API, interceptors, routing, services)
 */
export * from './infrastructure';

/**
 * UI types
 * @description UI component types (specific UI components)
 */
export * from './ui';
